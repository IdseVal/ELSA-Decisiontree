# Read by `next dev` only (docs/specs/application.md 17.4). The first start fills this
# folder from trees/; deleting it resets the development store. Production sets
# ELSA_DATA_DIR in the process environment.
ELSA_DATA_DIR=.elsa-data
NEXT_TELEMETRY_DISABLED=1
